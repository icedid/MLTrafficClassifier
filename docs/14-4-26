Based on your handwritten flowchart and our diagnostic session regarding the network traffic analysis implementation, here are your converted notes. They summarize the findings, theories, and identified limitations of the classification model.
Diagnostic Notes: Traffic Analysis & Model Verification

Date: 14/04/2026

Project Context: Validation of the DFI/DPI hybrid identification model.
1. Initial Test Results (Video Identification)

    Observations: Conducted three separate tests specifically for video traffic.

    Result: All tests showed correct predictions for the target IP address.

    Key Verification Rule: If the IP is present in the prediction output, the prediction is considered correct.

    Theory: The prediction logic is now accurate; the remaining confusion stems from a lack of complete understanding of the underlying data flow.

2. The "First 8 Packets" Rule & Its Impact

    Implementation: The model intercepted only the first 8 packets of each flow for statistical feature extraction.

    Observation: The system appears to "ignore" the rest of the session once the first 8 packets are processed.

    Technical Justification: According to the research, classification accuracy increases rapidly until N=8, after which it remains almost unchanged. Intercepting only these packets optimizes computing and storage resources.

3. Browser-Level Issues & Diagnosis

    Issue identified: Initial inconsistencies were likely caused by browser-side interference.

    Factors: Ad-blockers and other "weird stuff" occurring at the Layer 7 (Application Layer).

    Proposed Solution: Use a "bare" browser for future testing to ensure clean data collection without extension-based noise.

4. Identified Model Limitations

    Small Packet Sensitivity: The model has a documented "hard time" predicting activities characterized by small packet bursts.

    Chat Detection: The system currently does not pick up "chat" traffic effectively.

    Web Browsing: Testing showed that the "web-browsing" category is not yet working as expected.

    Network Storage: Successfully verified the "network-storage" category by testing uploads to a game drive.

5. Future Testing & Unresolved Questions

    Other Predictions: Need to investigate why the system generates "other" video predictions (false positives or background streams?).

    Category Expansion: Need to systematically test all other prediction types (e.g., instant messaging, mail service) to ensure the 54 features are being associated correctly.

    Flow Analysis: Further investigation is required to reconcile why some chat or small-packet flows are missed despite protocol-compliant stream logging.
